import { expect } from "chai";
import hre from "hardhat";
import { time } from "@nomicfoundation/hardhat-network-helpers";

describe("Arbitrade", function () {
  it("Should complete arb with minimum profit", async function () {
    // TODO: Write test
  });
});